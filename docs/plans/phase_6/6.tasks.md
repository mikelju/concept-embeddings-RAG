# Tasks Phase 6: Dense + Entity Navigation — end-to-end comparison

> Spec: `6.spec.md` | Plan: `6.0_dense_entity_navigation.md`
> Twenty-four implementation steps, so the atomic breakdown lives here.

<!--
Each task is one commit over at most 3-5 files.
[Story: HU-X] links it to the spec. [depends on TX] blocks it. [P] can run in parallel.
Tasks marked REAL run against the real data and produce versioned artifacts.
Every data-dependent test of T1-T20 uses dev questions only and is skipped when the data is absent.
No task before T23 builds a test query backend, evaluates a test question, or reads a test metric,
historical or new. Before T23 the two historical test files are identified (name, sha256, config) and
nothing more (plan D2). T21 and T22 are dev only. Order: dev -> freeze committed -> test.
-->

> **Temporal rule.** The first read of any test metric in this phase happens in T23, at step 5 of D14,
> after the freeze is committed (T22) and `dense` and A have been measured on test under it.

---

## T1: Phase 6 constants and the decision parameters [Story: HU-1, HU-2, HU-3, HU-5, HU-8, HU-10]

- **Files**: `src/concept_embeddings_rag/config.py`, `src/concept_embeddings_rag/decision_parameters.py`,
  `tests/test_config.py`, `tests/test_phase_6_decision_parameters.py`
- **Implementation note (2026-09-17)**: the decision parameters (`DECISION_*`, `N_TEST` copy, δ and its
  derivation, α, methods, `STATE_TABLE`, scenarios, sample rule) live in `decision_parameters.py`, not in
  `config.py`: `config.py` is on the Phase 3 selection import closure, whose guard forbids any string
  naming the test split. The historical files are keyed by role in `config.py` for the same reason. See
  the note under D13 of the plan; the spec is unaffected.
- **Action**: declare, before anything uses them: `REPLACEMENT_DIR`; `ENTITY_HOP_NAME = "entity-hop"`;
  `ENTITY_HOP_TYPES = ("entity",)`; `PHASE_6_SYSTEMS = ("dense", "hybrid-bm25", "hybrid-entity-hop")`;
  the pinned digests of D2 (selection, pilot, hop run, traces, extraction, prompt, node index,
  normalization version); the four historical result file names; the expected counts (19,366 units,
  96,416 entity nodes, 8 failed extractions, 163 paragraphs without an entity node, 152 pilot
  questions, 116 entity traces); `DECISION_METRIC`, `DECISION_BUDGET`, `DECISION_SPLIT`, `N_TEST`;
  `DELTA = Fraction(55, 2800)` with `M1_NUMERATOR`, `M1_DENOMINATOR`, `PRESERVED_FRACTION` and the two
  source file names; `DECISION_ALPHA = GATE_ALPHA`; the method descriptions and references; the
  eight-row `STATE_TABLE` with anomaly texts and open questions (`δ` escaped, emphasis kept, D21);
  the scenario definitions (D16, as decided in OI-3); the qualitative sample rule;
  `TRACE_WEIGHT_REL_TOL = 1e-12`, a relative tolerance applied with `math.fsum` (as decided in OI-2);
  `ensure_directories()` creates `REPLACEMENT_DIR`.
- **Verification**:
  - [x] Every value asserted against a literal in `tests/test_config.py`
  - [x] `DELTA == Fraction(M1_NUMERATOR, M1_DENOMINATOR) * (1 - PRESERVED_FRACTION)` and equals
        `Fraction(55, 2800)`
  - [x] Every anomaly and open-question text of `STATE_TABLE` occurs verbatim in `6.spec.md`, read from
        disk; a one-character mutation of a text fails the test (proof it can fail)
  - [x] The table has exactly the spec's eight `(S, V, N)` rows, each once
  - [x] Data-dependent: each pinned digest equals the artifact's recorded digest
  - [x] `config.py` and `decision_parameters.py` sources are ASCII; `mypy src` and `ruff check .` clean

## T2: The harness records per-question outcomes on request [Story: HU-6] [depends on T1]

- **Files**: `src/concept_embeddings_rag/evaluation/harness.py`,
  `tests/evaluation/test_harness_outcomes.py`
- **Action**: `QuestionOutcome` dataclass and the optional `outcomes` sink of D8; nothing else in the
  harness changes.
- **Verification**:
  - [x] Every existing test in `tests/evaluation/test_harness.py` passes unchanged
  - [x] With the sink, one record per question in the harness's loop order, carrying every budget's gold
        recall, Full Support, precision, units included, and recall@{2, 5, 10}
  - [x] The harness's `_mean` over the records, in that order, equals `RunResult.metrics` exactly
        (`==`, not approx), on a toy where precision takes fractional values. *Amended (D9 note): on
        Python 3.12 `sum` is compensated and no order-dependent toy exists; the order is kept and
        checked directly*
  - [x] The same retriever measured with and without the sink gives equal `metrics` and `cost` except
        latency
  - [x] `RunResult` has no new field; `test_the_measurer_and_the_interface_know_nothing_about_this_phase`
        still passes

## T3: `PerQuestionOutcomes` artifact [Story: HU-6, HU-9, HU-1] [depends on T2]

- **Files**: `src/concept_embeddings_rag/evaluation/outcomes.py`, `tests/evaluation/test_outcomes_artifact.py`
- **Action**: build from a `RunResult` and its outcome records; `questions` sorted by qid; `aggregates`
  recomputed in the pool's split order (D9); `run_result` identity; `freeze_digest` (required for test);
  digest; atomic write that never overwrites (a second file takes `-2`); loader verifying digest, the
  qid population, and aggregates against the named run result.
- **Verification**:
  - [x] A qid set missing one question of the split, or holding a foreign one, is refused
  - [x] Aggregates recomputed in pool order equal the run result exactly; the loader reorders the
        sorted-qid records into the pool's split order before averaging, checked by the order the
        values reach the mean. *Amended (D9 note): no toy can make the two orders disagree on Python
        3.12's compensated `sum`*
  - [x] A modified value, a mismatched run result, and a test outcome without `freeze_digest` are refused
  - [x] Writing twice never overwrites

## T4: `FusedRetriever` admits a second-stage component; A stays bit-identical [Story: HU-4, HU-2] [depends on T1] [P]

- **Files**: `src/concept_embeddings_rag/retrieval/fusion.py`,
  `tests/retrieval/test_fusion_second_stage.py`, `tests/retrieval/test_control_bit_identity.py`
- **Action**: `SECOND_STAGES`, the `SecondStage` protocol, `_in_fixed_order` accepting a stage by name
  and capability, `retrieve` calling dense once and handing its list to the stage (D4). `SECOND_SIGNALS`
  untouched.
- **Verification**:
  - [x] Every existing test in `tests/retrieval/test_fusion.py` and `test_control_parity.py` passes
        unchanged
  - [x] A staged hybrid is named `hybrid-entity-hop`, is a `Retriever`, and asks dense exactly once per
        query; the stage receives that very list object's content
  - [x] A stage whose name is not in `SECOND_STAGES`, a retriever under a stage name, and a stage under a
        signal name are all refused at construction
  - [x] Empty stage output: the weighted ranking equals `D(q)` for every `w > 0`; RRF adds no term
  - [x] Flat stage output: weighted abstains; RRF contributes by rank with ties by unit id
  - [x] Toy bit-identity: for both schemes and all 11 grid weights, the generalized A equals the Phase 3
        reference body `fuse([c.retrieve(q, k) for c in components], ...)`, scores included
  - [x] Data-dependent, dev only: the same equality with the real dense and BM25 retrievers over the 600
        dev questions, RRF and 11 weights (D5)
  - [x] Data-dependent: `data/selection/selection.json` still loads and verifies

## T5: `EntityHopStage` [Story: HU-3, HU-9, HU-5] [depends on T4]

- **Files**: `src/concept_embeddings_rag/retrieval/entity_hop.py`, `tests/retrieval/test_entity_hop.py`,
  `tests/retrieval/test_entity_hop_invariants.py`
- **Action**: the stage of D6: ordering check on `D(q)`, read set and `p1`, `second_hop.node_hop` with
  `ENTITY_HOP_TYPES`, `expand` returning the full `EntityExpansion`, `propose` returning hits,
  memoization by read list and depth.
- **Verification**:
  - [x] On a hand-computed toy index: `read`, `p1`, `E(q)` scores and order (ties by unit id), `P(q)`,
        truncation at the depth, no padding
  - [x] A paragraph sharing only a concept node with `p1` is never proposed
  - [x] `p1` with no entity node, and `p1` among failed extractions, give an empty list
  - [x] Output equals a direct `second_hop.node_hop(types=("entity",), ...)` call on the same inputs
  - [x] A `D(q)` out of order, or with a duplicate, is refused
  - [x] Memoized results are equal to fresh ones and are copies
  - [x] The constructor's parameters are exactly the index and the weights (inspected), and the read depth
        is `config.PILOT_READ_DEPTH`
  - [x] The Phase 3 scoring-path predicate (`identifiers_used` against `FORBIDDEN_ANNOTATIONS`) passes on
        `retrieval/entity_hop.py` and on `evaluation/second_hop.py`, and fails on a mutated copy

## T6: `fit_fusion_weight` serves a staged hybrid and keeps every point's metrics [Story: HU-5] [depends on T4]

- **Files**: `src/concept_embeddings_rag/evaluation/selection.py`,
  `tests/evaluation/test_fusion_fitting_stage.py`
- **Action**: widen the component type; add the observe-only `on_measure` hook (D10).
- **Verification**:
  - [x] Every existing test in `test_fusion_fitting.py`, `test_freeze.py`, `test_selection_artifact.py`
        passes unchanged
  - [x] A staged hybrid is fitted over the declared grid plus RRF, and the curve equals the one computed
        on paper for a toy
  - [x] The hook sees 12 results (RRF and 11 weights), each with all four budgets, and removing it
        changes no field of the returned `FusionFit`
  - [x] Ties to the larger dense weight and the strict-win rule for `weighted` hold on a staged fit
  - [x] Data-dependent: `data/selection/selection.json` still verifies

## T7: Verified inputs [Story: HU-1, HU-2, HU-3, HU-9] [depends on T1]

- **Files**: `src/concept_embeddings_rag/evaluation/replacement_inputs.py`,
  `tests/evaluation/test_replacement_inputs.py`, `tests/evaluation/replacement_fixtures.py` (a toy
  Phase 1-5 pipeline written by the project's own writers, reused by T8 and T16-T19; see the D3 note)
- **Action**: one loader returning a `VerifiedInputs` object: pool and manifest, split rule, token
  recount, corpus and question embeddings, `selection.json`, pilot, hop run, traces, extraction and node
  index against the pins, the expected counts (D2, D3). The four historical result files go through the
  **identity reader** of D2: existence under the pinned name, sha256 of the raw bytes, `system`, `split`,
  `created_at` and the HU-2 config keys, returned without `metrics` or `cost`. The **dev figures reader**
  (the `metrics` and `cost` of the two dev files) refuses any file whose `split` is not `dev`. This task
  holds **no reader for the figures of a test file**; that reader is written in T18. Dev question backend
  only; the test backend is built by T18's stage.
- **Verification**:
  - [x] One refusal per input on toy data: modified pool, a question on the wrong side of the split rule,
        a token count differing from the recount, a foreign embedding cache, a selection with another
        digest, a historical file with another `seed`/`top_k`/`tokenizer`/`fusion_weights`, a pilot, hop
        run, traces, extraction or node index with another digest
  - [x] The node index path comes from the pinned constant, never from the extraction summary; a summary
        whose `digest` is a path fragment is refused before any file is opened (SEC-022 not reachable)
  - [x] A count differing from the expected one stops with both numbers
  - [x] Toy: the identity reader's result has no `metrics` or `cost` field. A document whose `metrics` and
        `cost` are access-recording mappings reports zero accesses after identification. The dev figures
        reader refuses a toy file whose `split` is `test`
  - [x] Data-dependent: the real inputs verify; the two historical test files are identified (name, sha256,
        config) with zero accesses to their `metrics` or `cost`, recorded the same way. No assertion of
        this task reads a historical test figure or δ from a file

## T8: Continuity checks (a)-(d) [Story: HU-3] [depends on T5, T7]

- **Files**: `src/concept_embeddings_rag/evaluation/continuity.py`, `tests/evaluation/test_continuity.py`
- **Action**: for each pilot question, `D(q)` from the dense retriever and `E(q)` from the stage, then
  checks (a)-(d) of D11, each returning its count of agreements, its mismatches and its outcome. Nothing
  here reads a gold annotation except the pilot's `missing`, used only to compute (c).
- **Verification**:
  - [x] On a toy pilot, hop run and traces: all four checks pass, and each fails on its own when one value
        is altered (read list, positives, a score at 10 or 100, a trace's rank, score or node)
  - [x] Scores at 10 and 100 compared with `==`; trace scores and weights with the OI-2 tolerance
  - [x] Data-dependent (dev pilot): the function runs; its outcome is **not** asserted in the test, because
        the real check is the T21 run and a failing test would hide it

## T9: Control reproduction checks and the mode [Story: HU-2, HU-6] [depends on T3, T6, T7]

- **Files**: `src/concept_embeddings_rag/evaluation/control_reproduction.py`,
  `tests/evaluation/test_control_reproduction.py`
- **Action**: dev checks (curve, `rrf_score`, derived selection, config keys from the identities, the
  aggregates of `dense` and `hybrid-bm25` by the spec's list against the dev figures); the mode rule of
  D12; the test-side comparison (the same aggregates against historical test figures). Everything is a
  **pure function over figures the caller hands in**, and every check is `{name, expected, observed,
  passed}`. This module opens no file. On real data the dev checks are called by `replace-check` (T16);
  the test-side comparison is called only by `replace-test` (T18) at step 5 of D14.
- **Verification**:
  - [x] All checks pass on a toy whose "historical" figures are the toy's own runs: mode `reused`
  - [x] Any single altered figure, curve point or config key makes the mode `re-measured` and names the
        check
  - [x] Latency is never compared
  - [x] In `re-measured` mode the test-side comparison is recorded and returns without raising
  - [x] The test-side comparison is exercised on toy mappings only. No test of this task opens a
        historical test file, and the module has no file-reading call (source inspected)

## T10: Tango's score statistic and interval [Story: HU-7, HU-10] [depends on T1] [P]

- **Files**: `src/concept_embeddings_rag/evaluation/tango.py`, `tests/evaluation/test_tango.py`
- **Action**: `score_statistic(b, c, n, delta0)`, `lower_limit` and `upper_limit` by bracketed root finding,
  `non_inferiority(b, c, n, delta, alpha)` returning the statistic, its p-value, `L`, and `passed` with
  the consistency refusal of D15; the empty `PUBLISHED_EXAMPLE` slot. First check the D15 formula
  against Tango (1998)'s own equation and record the equation number in the docstring.
  *Not done, declared (D15 note): the paper was not available; the docstring says the formula is not
  verified against the paper, the restricted-MLE derivation is checked instead, and the equation check
  moves to T11.*
- **Verification**:
  - [x] At `delta0 = 0` the statistic equals `(b - c) / sqrt(b + c)`
  - [x] `L <= delta_hat <= U`; swapping `b` and `c` negates the interval; limits within `[-1, 1]`
  - [x] Defined at `b = c = 0` and at extreme splits; undefined arithmetic raises
  - [x] Agreement with an independent bisection on a grid of counts
  - [x] `passed` uses `Fraction` δ exactly; a case built on the boundary behaves as `L > -delta` strictly
  - [x] A forced disagreement between the statistic rule and the limit rule raises

## T11: Declared gap closed — the published worked example [Story: HU-7] [depends on T10]

- **Files**: `src/concept_embeddings_rag/evaluation/tango.py`, `tests/evaluation/test_tango.py`
- **Action**: transcribe the worked example's counts and printed limits from Tango, T. (1998),
  *Statistics in Medicine* 17(8), 891-908, with the table or page they come from, into
  `PUBLISHED_EXAMPLE`; assert the implementation reproduces them to the precision printed. The values
  come from the paper, read by the author or from a copy the author provides, and from nowhere else.
- **Verification**:
  - [ ] The D15 formula is checked against the paper's own equation, and its equation number recorded
        in the `tango.py` docstring (moved here from T10, which had no copy of the paper)
  - [ ] `PUBLISHED_EXAMPLE` names its source location
  - [ ] The implementation reproduces the printed limits to the printed precision
  - [ ] If it does not, the task stops and a deviation is written; the formula is never adjusted to fit
        silently
  - [ ] Must be done before T23; `replace-test` refuses while the slot is empty

## T12: Sign tests, the state procedure and `Phase6Decision` [Story: HU-10, HU-7, HU-2] [depends on T3, T10]

- **Files**: `src/concept_embeddings_rag/evaluation/replacement_decision.py`,
  `tests/evaluation/test_replacement_decision.py`
- **Action**: the `gate.sign_test` adapter for S and V; 2×2 counts; N from `tango.py`; `decide(s, v, n)`
  checked against the table; scenarios (D16); descriptive block; `Phase6Decision` saved once and loaded
  verified, computed only from outcome artifacts read back from disk and the freeze.
- **Verification**:
  - [ ] All eight `(S, V, N)` rows produce the table's state, route, anomalies and open question, texts
        taken from the freeze copy
  - [ ] Row 5 (`fail, pass, pass`) is PARTIAL P2 and row 7 (`fail, fail, pass`) is STOP
  - [ ] S and V p-values equal `scipy.stats.binomtest(..., alternative="greater")`; no discordant question
        gives p = 1 and a failed test
  - [ ] The spec's `reused`-mode argument: every split with a 55-question excess and at most 190
        dense-only successes gives p < 0.05
  - [ ] Ties stay in `n` for N and leave the sign tests' denominators
  - [ ] δ is the same in both modes; `n` equals the test population
  - [ ] Each scenario definition on a constructed case, including one where none holds
  - [ ] A second `save_decision` is refused; the decision function has no parameter for metric, budget,
        margin, α, method or population

## T13: `Phase6Freeze` [Story: HU-1, HU-5, HU-9, HU-2] [depends on T6, T9, T12]

- **Files**: `src/concept_embeddings_rag/evaluation/replacement_freeze.py`,
  `tests/evaluation/test_replacement_freeze.py`
- **Action**: the contract of D13, the ledger through `selection.fusion_decisions`, `n_dev_decisions` by
  mode, decision parameters copied from `config`, supersession (as decided in OI-4), save once, load verifying
  `evaluated_on`, digest, decision parameters and supersession chain.
- **Verification**:
  - [ ] A non-dev `RunResult` or outcome handed to the builder is refused
  - [ ] `n_dev_decisions` is 2 or 1 for B, 0 for the control in `reused`, the same count in `re-measured`
  - [ ] Each hybrid's selection re-derives from its stored curve by the Phase 3 rules
  - [ ] A decision-parameter copy differing from `config` in any field is refused on load
  - [ ] `protocol` holds every item of HU-1 AC 1; `control` holds mode and every check; `continuity` holds
        the four checks with counts; `entity_fit` and `control` hold the per-point metrics
  - [ ] The historical identity block holds the four file names, their sha256 and configs, and no
        `metrics` or `cost` of any historical file; a builder handed one is refused
  - [ ] The freeze satisfies `FrozenReport`; `check_freeze_precedes` accepts and refuses correctly
  - [ ] Overwrite refused; `freeze-2.json` refused without `supersedes` and an existing deviation path

## T14: Recording wrappers, diagnostics and traces [Story: HU-8, HU-4] [depends on T5, T13]

- **Files**: `src/concept_embeddings_rag/evaluation/entity_diagnostics.py`,
  `tests/evaluation/test_entity_diagnostics.py`
- **Action**: the wrappers, `EntityHopDiagnostics` and `EntityHopTrace` of D17, bound to the freeze, run
  and Phase 5 digests; loaders verifying bindings, node resolution and weight sums.
- **Verification**:
  - [ ] The same toy run with and without wrappers gives identical outcomes and aggregates
  - [ ] Each diagnostic on a toy built to hit it: empty, single candidate, flat (abstains under
        `weighted`, `null` under RRF), bottom tier, `p1` with no entity node, failed `p1`, overlap with
        ranks 11-100, B's and A's context units outside `D(q)`, `|E(q)| == 100`
  - [ ] One trace per qualifying unit; `entity` versus `dense-reorder` on a toy with both
  - [ ] A trace node absent from the index, of type `concept`, or whose weights miss the score beyond the
        tolerance, is refused on load
  - [ ] A recorded context that disagrees with the recorded outcome stops the build
  - [ ] Wrong freeze or run digest refused

## T15: Descriptive readout and the qualitative sample [Story: HU-7, HU-8, HU-6] [depends on T3, T13]

- **Files**: `src/concept_embeddings_rag/evaluation/readout.py`, `tests/evaluation/test_readout.py`
- **Action**: the tables of D18 from outcome artifacts, labelled descriptive; failure analysis through
  `CrossTab`; bridge-like subgroup; the sampling rule applied on test.
- **Verification**:
  - [ ] Three 2×2 tables whose four cells sum to `n` and agree with the decision's counts
  - [ ] Failure lists equal hand-computed qids on a toy
  - [ ] The subgroup rule selects exactly the questions with a gold outside `read(q)`; on a toy pilot the
        comparison with the pilot's qids reports equality and a difference
  - [ ] The sample takes the first 5 by ascending qid per group, fewer when a group is smaller
  - [ ] Nothing in the readout is read by `replacement_decision.py` (import check)

## T16: Stage `replace-check` [Story: HU-1, HU-2, HU-3] [depends on T8, T9, T14]

- **Files**: `src/concept_embeddings_rag/evaluation/replacement_run.py`,
  `src/concept_embeddings_rag/cli.py`, `tests/test_cli_replacement.py`
- **Action**: steps 1-3 of D11; `checks-dev.json` written with digest; non-zero exit on any failed check.
- **Verification**:
  - [ ] On a toy pipeline in `tmp_path`: passing inputs write `checks-dev.json` with mode `reused`
  - [ ] A continuity failure exits before any fit is run (the fitting function is never called)
  - [ ] A control mismatch writes `checks-dev.json` with the failed checks and exits non-zero; no
        `hybrid-entity-hop` file exists
  - [ ] Only dev questions reach the harness; no test query backend is built
  - [ ] The historical test files are identified and their `metrics` and `cost` never accessed (spy)
  - [ ] `cli.py` gains no `fit_fusion_weight` call and no `FusedRetriever` construction (Phase 3
        structural tests still count 2 and 1)

## T17: Stage `replace-freeze` [Story: HU-5, HU-6, HU-8, HU-9, HU-2] [depends on T15, T16]

- **Files**: `src/concept_embeddings_rag/evaluation/replacement_run.py`,
  `src/concept_embeddings_rag/cli.py`, `tests/test_cli_replacement.py`
- **Action**: steps 4-8 of D11; `--control-mode re-measured --deviation <path>` of D12.
- **Verification**:
  - [ ] Refuses without a passing `checks-dev.json`, and refuses `re-measured` without an existing
        deviation document
  - [ ] Writes B's dev result and outcomes, the reproducibility block, the freeze, then dev diagnostics,
        traces and readout bound to the freeze
  - [ ] B's selected-point aggregates equal the fit's reading at that point
  - [ ] A toy where B's fit lands on `w = 1.0`, and one where RRF wins, both freeze normally
  - [ ] A second invocation refuses: the freeze exists
  - [ ] Only dev questions reach the harness, no test query backend is built, and no test metric is read
        (the same spy as T16)

## T18: Stage `replace-test`, the ordered test protocol [Story: HU-1, HU-2, HU-9, HU-10, HU-6] [depends on T11, T12, T15, T17]

- **Files**: `src/concept_embeddings_rag/evaluation/replacement_run.py`,
  `src/concept_embeddings_rag/cli.py`, `tests/test_cli_replacement.py`
- **Action**: the nine steps of D14. This task also writes, in `replacement_run.py`, the **only reader of
  historical test figures** (D2). It requires the loaded, verified freeze, re-checks each file's bytes
  against the sha256 the freeze recorded, and is called once, at step 5, where the δ confirmation of
  D13 also runs.
- **Verification**:
  - [ ] Refuses without a verified freeze, with an altered decision-parameter copy, with an empty
        `PUBLISHED_EXAMPLE`, or when `decision.json` or any `hybrid-entity-hop` test file exists
  - [ ] Call order recorded on a toy: `dense`, `hybrid-bm25`, historical test figures read, reproduction
        check, `hybrid-entity-hop`. The reader is never called before `dense` and A are written, and a
        refusal at step 1 leaves it uncalled
  - [ ] The reader refuses without a freeze, and when a file's bytes differ from the sha256 recorded in the
        freeze
  - [ ] A historical Full Support pair whose difference is not `M1_NUMERATOR` stops before B, in both modes
  - [ ] A failed reproduction check in `reused` mode writes `test-reproduction.json`, exits non-zero, and
        leaves no B result, outcome, diagnostic or decision
  - [ ] A second run refuses on the existing `dense` test result; with `--control-mode re-measured
        --deviation <existing path>` it re-reads `dense` and A into `-2` files and then evaluates B once
  - [ ] Every test result carries `freeze_digest`, `frozen_at`, pool hash, dense and BM25 identities,
        B's node-index digest, `code_version`, `"phase": 6`; `check_freeze_precedes` holds
  - [ ] The decision is computed from outcomes read back from disk

## T19: Contamination and parity audit [Story: HU-9, HU-1, HU-3, HU-4, HU-5, HU-2] [depends on T18]

- **Files**: `tests/evaluation/test_replacement_contamination.py`, `tests/retrieval/test_replacement_parity.py`
- **Action**: the assertions of HU-9 and "the same opportunity to contribute" in two modules.
- **Verification**:
  - [ ] Every dev door of `replacement_run.py` refuses a test question, and one hidden among dev ones
  - [ ] Structural check: no Phase 6 module outside `replacement_run.py`'s test sequence reads the
        `metrics` or `cost` of a historical test file. The reader of historical test figures is referenced
        at exactly one call site, step 5 of the test sequence, and a tampered source that calls it from a
        dev stage fails the check
  - [ ] No Phase 6 test module opens a historical test file other than through the identity reader
        (source inspection over `tests/`)
  - [ ] Shuffling gold annotations leaves `D(q)`, `E(q)` and every fused ranking unchanged
  - [ ] No Phase 6 module imports the Anthropic SDK or `read_api_key`; the stages run with
        `ANTHROPIC_API_KEY` unset
  - [ ] No Phase 6 module writes under `data/nodes/`, `data/extraction/`, `data/navigation/`,
        `data/pilot/`, `data/selection/` or `data/results/` (write calls inspected)
  - [ ] The two `fit_fusion_weight` calls in `replacement_run.py` pass the same keywords, and the two hybrid
        constructions the same scheme and weight keywords; a tampered source fails the check
  - [ ] Both hybrids use `fuse`, `RRF_K`, the same grid, `top_k = EVALUATION_TOP_K`, dense first

## T20: Versioning and the secrets gate [Story: HU-9] [depends on T1] [P]

- **Files**: `.gitignore`, `.secrets.baseline`, `.github/detect_secrets_filters.py`,
  `tests/test_security_gate.py`, `docs/security/README.md`
- **Action**: version `data/replacement/*.json`. Write the content filter of D20: path, detector, line
  shape against the key allowlist, and value; standard library only. Register it in `.secrets.baseline`
  `filters_used`. **Phase 5's path exclusion is left unchanged, and no path exclusion is added.** Record
  the subsection D20 describes in the security README.
- **Verification**:
  - [ ] Function level, skipped: every allowlisted key at every allowed length, bare hex of length 16 and
        24, either path separator
  - [ ] Function level, reported:
        - the same lines under `data/replacement/nested/`, in `data/replacement/x.py`, under
          `xdata/replacement/` and under `src/`;
        - a key off the allowlist, a wrong length, uppercase hex;
        - extra content on the line;
        - a bare 64-character element;
        - a finding of another detector on an allowed line;
        - a `secret` different from the line's value
  - [ ] End to end (skipped without the `security` group): the real scanner, configured from the real
        baseline and run from the root of a temporary repository-shaped tree, reports exactly the planted
        cases that must still be reported
  - [ ] `filters_used` holds exactly one custom filter, this one, and Phase 5's `should_exclude_file`
        pattern unchanged; `test_the_path_exclusion_covers_only_the_phase_5_id_artifacts` lists
        `data/replacement/x.json` among the paths that pattern must still scan
  - [ ] The filter module imports only the standard library, and `ruff check .` covers it
  - [ ] Allowlist starts from the key names of the spec's data contracts only (D20). A data-dependent test,
        skipped while `data/replacement/` holds no JSON, requires every allowlisted key to occur in some
        artifact there: once T21-T23 have written them, a key no writer emits fails and is removed
  - [ ] `detect-secrets-hook --baseline .secrets.baseline $(git ls-files)` passes locally over the full
        tracked tree
  - [ ] The security README records the measurement, why there is no path exclusion, the four conditions,
        the allowlist, what stays scanned, how the filter fails, and the regeneration command with
        `--filter`

## T21: REAL — `cer replace-check` [Story: HU-2, HU-3] [depends on T16, T20]

- **Files**: `data/replacement/checks-dev.json`, `data/replacement/run-{dense,hybrid-bm25}-dev-*.json`,
  `data/replacement/outcomes-{dense,hybrid-bm25}-dev.json`
- **Action**: `uv run cer replace-check`; read the continuity and control checks. **Checkpoint.**
- **Verification**:
  - [ ] Inputs verify, including the tokenizer recount and the expected counts
  - [ ] Continuity (a)-(d) pass on 152 questions and 116 traces, or the phase stops here and a `6.Y`
        deviation is written before anything else runs
  - [ ] The control curve, `rrf_score` and all dev aggregates reproduce: mode `reused`; or the deviation
        is written and T22 runs with `--control-mode re-measured`
  - [ ] No test question evaluated and no test metric read: the two historical test files identified only
  - [ ] `detect-secrets-hook` passes over the tracked tree with the new artifacts staged, with no new
        baseline entry; artifacts committed

## T22: REAL — `cer replace-freeze`; freeze committed before any test read [Story: HU-5, HU-8, HU-9] [depends on T17, T21]

- **Files**: `data/replacement/freeze.json`, B's dev result and outcomes, `diagnostics-dev.json`,
  `traces-dev.json`, `readout-dev.json`
- **Action**: `uv run cer replace-freeze`; read B's selection as the finding it is, whatever it is; commit.
- **Verification**:
  - [ ] The freeze verifies on load; its decision parameters equal the spec's
  - [ ] Reproducibility block: every dev per-question outcome reproduced, or the source of nondeterminism
        recorded and the phase stops
  - [ ] B's fit is reported unchanged if it selects `w = 1.0` or RRF: no re-fit, no early stop
  - [ ] Dev bridge-like subgroup compared with the 152 pilot questions; any difference reported
  - [ ] Dev only: no test question evaluated, no test metric read, no test query backend built
  - [ ] The freeze records the four historical files' sha256 and configs and no historical test figure
  - [ ] `detect-secrets-hook` passes with the new artifacts staged, with no new baseline entry
  - [ ] **The freeze and the dev artifacts are committed** before T23 starts

## T23: REAL — `cer replace-test`, once [Story: HU-1, HU-2, HU-6, HU-8, HU-9, HU-10] [depends on T11, T22]

- **Files**: `data/replacement/` test results, outcomes, `diagnostics-test.json`, `traces-test.json`,
  `readout-test.json`, `decision.json` (or `test-reproduction.json` on a stop)
- **Action**: after the author confirms that T11 is closed and T22 is committed, `uv run cer
  replace-test`. Nothing is re-run with another metric, budget, margin, α, method, comparator or
  population.
- **Verification**:
  - [ ] The freeze committed in T22 is the one `replace-test` loads (digest matches the committed file)
  - [ ] Order observed in the log: `dense`, `hybrid-bm25`, historical test figures read with the δ
        confirmation, reproduction check, `hybrid-entity-hop`. This is the first test metric the phase
        reads
  - [ ] Reproduction passes (`reused`), or the run stopped with no B figure and a `6.Y` deviation is
        written before any re-run
  - [ ] B evaluated once; `decision.json` written with every statistic, state, route, anomalies, open
        question and scenarios
  - [ ] `L > -delta` and the statistic rule agree (the stage refused otherwise)
  - [ ] `detect-secrets-hook` passes with the new artifacts staged, with no new baseline entry; artifacts
        committed

## T24: Results and the master plan [Story: HU-7, HU-10] [depends on T23]

- **Files**: `docs/plans/phase_6/6.results.md`, `docs/plans/0_master_plan.md`
- **Action**: write the results from the artifacts: dev and test tables, the three 2×2 tables, every
  decision statistic, the state with its route, anomalies and verbatim open question, the scenario(s),
  descriptive readings labelled as deciding nothing, failure analysis, subgroup, diagnostics, the
  qualitative sample read by the frozen rule, cost; STOP with its scope if STOP. Update the master plan
  by the state.
- **Verification**:
  - [ ] Every figure names its artifact and the command that regenerates it
  - [ ] Any replacement claim quotes `decision.json`'s `state`, never an aggregate
  - [ ] No state opens Phase 7 or 8 in the master plan; any follow-up is recorded as the author's to decide
  - [ ] The founding decisions table untouched

---

## Status summary

| Task | Story | Status |
|---|---|---|
| T1 | HU-1, HU-2, HU-3, HU-5, HU-8, HU-10 | Done |
| T2 | HU-6 | Done |
| T3 | HU-6, HU-9, HU-1 | Done |
| T4 | HU-4, HU-2 | Done |
| T5 | HU-3, HU-9, HU-5 | Done |
| T6 | HU-5 | Done |
| T7 | HU-1, HU-2, HU-3, HU-9 | Done |
| T8 | HU-3 | Done |
| T9 | HU-2, HU-6 | Done |
| T10 | HU-7, HU-10 | Done |
| T11 | HU-7 | Pending — declared gap, needs the paper |
| T12 | HU-10, HU-7, HU-2 | Pending — unblocked (OI-3 decided 2026-09-17) |
| T13 | HU-1, HU-5, HU-9, HU-2 | Pending — unblocked (OI-4 decided 2026-09-17) |
| T14 | HU-8, HU-4 | Pending |
| T15 | HU-7, HU-8, HU-6 | Pending |
| T16 | HU-1, HU-2, HU-3 | Pending |
| T17 | HU-5, HU-6, HU-8, HU-9, HU-2 | Pending — unblocked (OI-1 decided 2026-09-17) |
| T18 | HU-1, HU-2, HU-9, HU-10, HU-6 | Pending |
| T19 | HU-9, HU-1, HU-3, HU-4, HU-5, HU-2 | Pending |
| T20 | HU-9 | Pending |
| T21 | HU-2, HU-3 | Pending — REAL, checkpoint |
| T22 | HU-5, HU-8, HU-9 | Pending — REAL, freeze committed |
| T23 | HU-1, HU-2, HU-6, HU-8, HU-9, HU-10 | Pending — REAL, test read once |
| T24 | HU-7, HU-10 | Pending |

After T24: `/7-verificar`, then `/8-auditar` (mandatory: new artifact deserialization and a loader
path into Phase 5's node index), then `/9-documentar`.
