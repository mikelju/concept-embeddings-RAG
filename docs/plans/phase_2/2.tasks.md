# Tasks Phase 2: Concept space - dictionary and matrix X

> Spec: `2.spec.md` | Plan: `2.0_concept_space.md`
> Eighteen implementation steps, so the atomic breakdown lives here.

<!--
Each task is one commit over at most 3-5 files.
[Story: HU-X] links it to the spec. [depends on TX] blocks it. [P] can run in parallel.
-->

---

## T0: Amend HU-1 of the spec for decision D1 [Story: HU-1] - DONE

- **Files**: `docs/plans/phase_2/2.spec.md`, `docs/plans/0_master_plan.md`
- **Action**: replace `positive_dict=True` with `positive_dict=False` in HU-1 and in the Phase 2
  bullet of the master plan, recording the measurement that forced it (51.3% of embedding energy in
  negative coordinates; 1.59 active per unit and 56% dead atoms in the constrained fit).
  **Approved by the author during planning.**
- **Verification**:
  - [x] Neither document still asks for a non-negative dictionary
  - [x] The evidence is in the documents, not only in this plan

## T1: Phase 2 constants [Story: HU-1, HU-2, HU-4, HU-6] [depends on T0]

- **Files**: `src/concept_embeddings_rag/config.py`, `tests/test_config.py`
- **Action**: `CONCEPT_K_SWEEP`, `CONCEPT_SEED`, `SPARSITY_TARGET_BAND`, `SPARSITY_FLOOR`,
  `MERGE_COSINE_THRESHOLD`, `MAX_MERGED_FRACTION`, `CONCEPTS_DIR`, `LABELING_MODEL`,
  `LABEL_EVIDENCE_UNITS`, `LABELED_K`. Nothing inline anywhere else.
- **Verification**:
  - [x] The four K values, the seed and the floor of 3 are asserted in the test
  - [x] `mypy src` and `ruff check .` clean

## T2: `ConceptDictionary` artifact [Story: HU-1, HU-3] [depends on T1]

- **Files**: `src/concept_embeddings_rag/concepts/__init__.py`,
  `src/concept_embeddings_rag/concepts/dictionary.py`,
  `tests/concepts/test_dictionary_artifact.py`
- **Action**: the dataclass of the spec's data contract, the configuration key derived from
  (K, seed, alpha, `unit_set_hash`, model, revision), `.npz` + JSON sidecar save/load with
  `allow_pickle=False`, and validation on load against the pool it claims to describe.
- **Verification**:
  - [x] Round-trip preserves atoms bit for bit
  - [x] A changed parameter yields a different filename, never an overwrite
  - [x] Loading a dictionary whose `unit_set_hash` does not match the pool aborts

## T3: Induction by non-negative sparse coding [Story: HU-1] [depends on T2]

- **Files**: `src/concept_embeddings_rag/concepts/dictionary.py`,
  `tests/concepts/test_induction.py`
- **Action**: `induce_dictionary(vectors, k, seed, alpha, ...)` over
  `MiniBatchDictionaryLearning(fit_algorithm="cd", transform_algorithm="lasso_cd",
  positive_code=True, positive_dict=False, random_state=seed)`, atoms L2-normalized on the way out,
  provenance recorded. The fitted estimator is never serialized - only `components_`.
- **Verification**:
  - [x] Atoms come back unit-norm, float32, shape [K, 384]
  - [x] Test runs on small synthetic vectors, no model download, no cached corpus
  - [x] The `ConvergenceWarning` from coordinate descent is either resolved or explicitly accepted
        with a recorded reason

## T4: Bit-identical reproducibility [Story: HU-1] [depends on T3]

- **Files**: `tests/concepts/test_reproducibility.py`,
  `src/concept_embeddings_rag/concepts/dictionary.py`
- **Action**: assert two inductions with the same seed produce the same SHA-256 of the atom array.
  If BLAS threading breaks it, pin thread count inside the induction and record why.
- **Verification**:
  - [x] The digests match
  - [x] A different seed produces a different digest (the test proves it can fail)

## T5: Sparsity calibration [Story: HU-2] [depends on T3]

- **Files**: `src/concept_embeddings_rag/concepts/coding.py`,
  `tests/concepts/test_calibration.py`
- **Action**: search `alpha` per K until the mean active concepts per unit lands in the declared
  band, measured over a seeded subsample of unit embeddings only. The chosen `alpha` and the
  achieved mean travel with the artifact.
- **Verification**:
  - [x] The search is deterministic given the seed
  - [x] It reads no question, no gold and no split label
  - [x] A synthetic case whose target is unreachable fails loudly instead of returning a silent miss

## T6: Coding the corpus to `X` [Story: HU-2] [depends on T5]

- **Files**: `src/concept_embeddings_rag/concepts/coding.py`, `tests/concepts/test_coding.py`
- **Action**: batched `transform`, each batch converted to CSR and stacked; compute achieved mean
  active per unit and relative reconstruction error against the original embeddings.
- **Verification**:
  - [x] Every entry of `X` is non-negative
  - [x] Peak memory tracks the batch, not the corpus (asserted through the batch size, not measured)
  - [x] Reconstruction error is computed against the same vectors that were coded

## T7: `ConceptMatrix` artifact and the rejection rule [Story: HU-2, HU-3] [depends on T6]

- **Files**: `src/concept_embeddings_rag/concepts/coding.py`,
  `tests/concepts/test_matrix_artifact.py`
- **Action**: persist the CSR components by hand as `.npz` with a JSON sidecar carrying
  `weight_semantics`, `view`, `dictionary_key`, achieved sparsity and reconstruction error;
  validate `unit_ids` against the pool on load; refuse to ship a fit below 3 mean active per unit.
- **Verification**:
  - [x] `weight_semantics` is present in the file, not only in the documentation
  - [x] A mismatched `unit_ids` list aborts the load
  - [x] A fit at 2.5 active per unit raises rather than writing an artifact

## T8: The `row_normalized` view [Story: HU-2] [P] [depends on T7]

- **Files**: `src/concept_embeddings_rag/concepts/coding.py`, `tests/concepts/test_views.py`
- **Action**: a pure function over the raw matrix; zero rows stay zero rather than dividing by zero.
- **Verification**:
  - [x] Rows of the normalized view sum to 1, orphan rows excepted
  - [x] The raw matrix is not mutated

## T9: Deduplication and its merge log [Story: HU-4] [depends on T7]

- **Files**: `src/concept_embeddings_rag/concepts/dedup.py`, `tests/concepts/test_dedup.py`
- **Action**: detect pairs above the declared cosine threshold, merge each group into one
  L2-normalized replacement atom, write a versioned merge log (members, pairwise similarities,
  resulting index), and **recompute `X`** against the merged dictionary.
- **Verification**:
  - [x] Two deliberately duplicated atoms merge; two unrelated ones do not
  - [x] The recomputed `X` is a transform output, not a column sum of the old one
  - [x] Merging beyond `MAX_MERGED_FRACTION` is reported as a finding about K, not swallowed

## T10: Space diagnostics [Story: HU-5] [depends on T9]

- **Files**: `src/concept_embeddings_rag/concepts/diagnostics.py`,
  `tests/concepts/test_diagnostics.py`
- **Action**: the six statistics of the data contract into one JSON artifact per dictionary:
  active per unit, units per concept, dead atoms, orphan units, top units per concept, top
  co-activations.
- **Verification**:
  - [x] Computed over the whole pool, with no split parameter to break it down by
  - [x] Orphan units are listed by id, never merely counted
  - [x] Co-activation is symmetric and excludes the concept itself

## T10b: Dictionary quality against a null baseline [Story: HU-7] [depends on T10]

> Added after T0-T17 were planned, on the author's review of the phase: HU-5 can report a space
> where every concept is junk and no statistic says so. It lands **before T17**, so the real sweep
> writes the quality block from the first run and `2.results.md` is not rewritten later.

- **Files**: `src/concept_embeddings_rag/config.py`,
  `src/concept_embeddings_rag/concepts/diagnostics.py`,
  `src/concept_embeddings_rag/cli.py`, `tests/concepts/test_diagnostics.py`,
  `tests/test_config.py`, `tests/test_cli.py`
- **Action**: per-concept coherence, atom alignment and mass concentration, plus the seeded random
  null baseline they are read against, into the `quality` block of the existing diagnostics
  artifact. `compute_diagnostics` takes the cached Phase 1 embeddings and the atoms; the `induce`
  stage passes both, and recomputes a diagnostics file that predates the block instead of skipping
  it. Nothing is pruned.
- **Verification**:
  - [ ] The null baseline is measured on this pool, and the artifact carries its mean, sd and seed
  - [ ] Same matrix, same embeddings, same seed - same numbers
  - [ ] A concept with fewer than the declared minimum of units is undefined, not zero
  - [ ] A synthetic space with one coherent and one deliberately mixed concept ranks them in that
        order, so the metric is shown to be able to fail
  - [ ] `X`, the dictionary and the concept count come out of the diagnostic untouched

## T11: The anti-pickle guard [Story: HU-3] [P]

- **Files**: `tests/concepts/test_no_pickle.py`
- **Action**: scan `src/concept_embeddings_rag/concepts/` for `pickle`, `joblib.dump`,
  `joblib.load` and `allow_pickle=True`, so the rule survives a refactor rather than reviewer memory.
- **Verification**:
  - [x] The test fails when a forbidden call is introduced deliberately
  - [x] It reads source text, so a dynamic import cannot slip past a mere import check

## T12: CLI stage `induce` [Story: HU-1, HU-2, HU-4, HU-5] [depends on T10]

- **Files**: `src/concept_embeddings_rag/cli.py`, `tests/test_cli.py`
- **Action**: sweep the four K end to end - induce, calibrate, code, deduplicate, diagnose - refusing
  to run without cached embeddings and naming `embed`. Print per-K wall time and stop the sweep if it
  exceeds the declared budget.
- **Verification**:
  - [x] Missing embeddings produce `[ERROR] ... run 'embed' first`, not a numpy traceback
  - [x] A second run over unchanged inputs recomputes nothing
  - [x] Every message is ASCII

## T13: Optional dependency group and `.env.example` [Story: HU-6] [P] [depends on T1]

- **Files**: `pyproject.toml`, `.env.example`, `tests/concepts/test_offline_path.py`
- **Action**: add `anthropic` and `python-dotenv` in a `labeling` group, verify they resolve on
  ARM64 alongside the existing `httpx`, and add `.env.example` with the variable name and no value.
- **Verification**:
  - [x] `uv sync` without the group leaves induction, coding, dedup and diagnostics fully working
  - [x] The test proves the numerical path imports neither package
  - [x] `.env` stays git-ignored; `.env.example` holds no value

## T14: Concept labelling with a disk cache [Story: HU-6] [depends on T13, T10]

- **Files**: `src/concept_embeddings_rag/concepts/labeling.py`,
  `tests/concepts/test_labeling.py`
- **Action**: build the prompt from a concept's top activating units, call `claude-sonnet-5` with
  thinking disabled and a structured `{name, gloss}` output, cache each response on disk keyed by a
  hash of the prompt content, accumulate real token cost, and write the `ConceptLabels` artifact
  including its non-reproducibility note. Typed-exception chain per decision D9.
- **Verification**:
  - [x] A second run over an unchanged dictionary makes zero calls (asserted with a fake client)
  - [x] The cache key contains no key material and no environment
  - [x] The artifact holds no secret, and the test asserts it
  - [x] Tests use a fake client; no network, no spend

## T15: CLI stage `label` [Story: HU-6] [depends on T14]

- **Files**: `src/concept_embeddings_rag/cli.py`, `tests/test_cli.py`
- **Action**: label K = 2,048 only; exit with a clear message naming `ANTHROPIC_API_KEY` when it is
  absent, before any HTTP client is constructed. Print the estimated cost and ask nothing else.
- **Verification**:
  - [x] Without a key: a one-line `[ERROR]`, exit code non-zero, no traceback
  - [x] With a fake client: writes the artifact and reports actual cost

## T16: The labels stay out of the retrieval path [Story: HU-6] [P] [depends on T14]

- **Files**: `tests/concepts/test_labels_not_in_retrieval_path.py`
- **Action**: assert that no module under `retrieval/`, `evaluation/`, `dictionary.py`, `coding.py`,
  `dedup.py` imports `labeling` or reads the labels artifact.
- **Verification**:
  - [x] The test names the modules explicitly, so a new retrieval module must be added deliberately
  - [x] It fails if an import is introduced

## T17: The real run and its reading [Story: all] [depends on T12, T15]

- **Files**: `docs/plans/phase_2/2.results.md`, `data/results/**`
- **Action**: run the sweep, deduplicate, diagnose, label K = 2,048, and read the space critically -
  including the diagnostics that look wrong. Record wall time and real spend.
- **Verification**:
  - [ ] Every recorded figure carries the configuration that produced it
  - [ ] Sparsity, reconstruction error, dead atoms, orphan units and merge counts are reported per K
  - [ ] Coherence against the null baseline is reported per K, and the concepts below the null are
        read rather than only counted
  - [ ] The K = 2,048 labels are read worst-coherence first, so the critical reading starts at the
        concepts most likely to be junk instead of at an average that always looks reasonable
  - [ ] No gap is filled with an estimate; a missing datum is declared as missing
  - [ ] The document states plainly that none of these numbers is evidence for the hypothesis

---

## Status summary

| Task | Story | Status |
|-------|-------|--------|
| T0 | HU-1 | Done |
| T1 | HU-1, HU-2, HU-4, HU-6 | Done |
| T2 | HU-1, HU-3 | Done |
| T3 | HU-1 | Done |
| T4 | HU-1 | Done |
| T5 | HU-2 | Done |
| T6 | HU-2 | Done |
| T7 | HU-2, HU-3 | Done |
| T8 | HU-2 | Done |
| T9 | HU-4 | Done |
| T10 | HU-5 | Done |
| T10b | HU-7 | Pending |
| T11 | HU-3 | Done |
| T12 | HU-1, HU-2, HU-4, HU-5 | Done |
| T13 | HU-6 | Done |
| T14 | HU-6 | Done |
| T15 | HU-6 | Done |
| T16 | HU-6 | Done |
| T17 | all | Pending |
