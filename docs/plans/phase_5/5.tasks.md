# Tasks Phase 5: Text-derived concepts — navigation pilot

> Spec: `5.spec.md` | Plan: `5.0_text_derived_concepts.md`
> Sixteen implementation steps, so the atomic breakdown lives here.

<!--
Each task is one commit over at most 3-5 files.
[Story: HU-X] links it to the spec. [depends on TX] blocks it. [P] can run in parallel.
Tasks marked REAL run against the real data or the real API and produce versioned artifacts.
-->

---

## T1: Phase 5 constants [Story: HU-1, HU-2, HU-4, HU-7]

- **Files**: `src/concept_embeddings_rag/config.py`, `tests/test_config.py`
- **Action**: declare, before anything uses them: `PILOT_DIR`, `EXTRACTION_DIR`, `NODES_DIR`,
  `NAVIGATION_DIR`; `PILOT_READ_DEPTH = 10`; `SECOND_HOP_DEPTHS = (10, 100)`; `EXTRACTION_MODEL =
  "claude-sonnet-5"` with its published per-token rates and `BATCH_PRICE_FACTOR = 0.5`;
  `EXTRACTION_MAX_TOKENS = 1024`; `MAX_NODES_PER_TYPE = 50`; `MAX_NODE_CHARS = 120`;
  `EXTRACTION_SAMPLE_SIZE = 20`; `EXTRACTION_ESTIMATE_MARGIN = 1.5`;
  `EXTRACTION_COST_CEILING_USD = 25.0`; `EXTRACTION_BATCH_SIZE = 5000`;
  `EXTRACTION_FAILURE_FINDING = 0.01`; `NODE_TYPES = ("entity", "concept")`; `NAVIGATION_ARMS =
  ("entity", "concept", "entity+concept")`; `NORMALIZATION_VERSION = "normalization-v1"`;
  `GATE_METRIC_DEPTH = 10`; `GATE_ALPHA = 0.05`; `GATE_COMPARATOR = "bm25-question"`;
  `TRACES_READ_PER_ARM = 5`; `REFERENCE_CONCEPT_K = 512`.
- **Verification**:
  - [x] Each value asserted in the test, the ceiling and the gate constants among them
  - [x] `GATE_COMPARATOR` names a hop the protocol defines, asserted after T2
  - [x] `ensure_directories()` creates the four new directories
  - [x] `mypy src` and `ruff check .` clean

## T2: The second-hop protocol moves into `src` [Story: HU-5] [depends on T1]

- **Files**: `src/concept_embeddings_rag/evaluation/second_hop.py`,
  `scripts/second_hop_diagnostic.py`, `tests/evaluation/test_second_hop.py`
- **Action**: functions for the dense order and read top-10, `p1`, read-excluded ranking (positive-only
  or not), BM25 ranking with read excluded, and the five existing hops, each returning ranked rows to
  depth 100. The script keeps its CLI and output and calls them (D8).
- **Verification**:
  - [x] Toy-data tests: read paragraphs never ranked, positive-only honoured, depths respected
  - [x] Data-dependent test (skipped without the pool): the script reproduces the committed
        `data/diagnostics/second_hop-dev.json` hit rates, reach and structure exactly
  - [x] Every existing test still passes

## T3: `PilotSet` and `cer pilot` [Story: HU-1, HU-8] [depends on T2]

- **Files**: `src/concept_embeddings_rag/evaluation/pilot.py`, `src/concept_embeddings_rag/cli.py`,
  `tests/evaluation/test_pilot.py`
- **Action**: build the pilot from dev questions with the HU-1 rule, sorted by question id; save
  atomically with a digest; load verifying digest, pool hash and invariants; refuse any non-dev
  question through `check_dev_only`. `cer pilot` writes `data/pilot/pilot.json` and never overwrites
  an existing one.
- **Verification**:
  - [x] Toy pool: the rule selects exactly the questions with a gold outside the read depth
  - [x] Invariants enforced on load: `p1 == read[0]`, `missing` non-empty, gold and not read
  - [x] A modified artifact and a foreign pool hash are both refused
  - [x] A test-split question passed to the builder is refused
  - [x] Building twice yields byte-identical artifacts

## T4: REAL — freeze the pilot [Story: HU-1] [depends on T3]

- **Files**: `data/pilot/pilot.json`, `.gitignore`, `.secrets.baseline`, `docs/security/README.md`
- **Action**: run `uv run cer pilot`; version `data/pilot/`; add the D12 exclusion for the id-bearing
  Phase 5 artifact directories and record why.
- **Verification**:
  - [x] 152 questions and 158 missing paragraphs, or the discrepancy explained before continuing
  - [x] `detect-secrets-hook` passes locally over the full tracked tree with the new baseline
  - [x] Committed before any extraction code runs against the API

## T5: Extraction request, validation and cache [Story: HU-2, HU-8] [depends on T1] [P]

- **Files**: `src/concept_embeddings_rag/nodes/__init__.py`,
  `src/concept_embeddings_rag/nodes/extraction.py`, `tests/nodes/test_extraction.py`
- **Action**: the frozen prompt template and its digest; the D3 schema; `build_request(unit)` taking
  a unit and nothing else; `validate_answer` enforcing the schema and bounds and returning an
  `ExtractionRecord`, with refusals, `max_tokens` stops, non-JSON and out-of-bounds answers recorded
  as failures; the per-paragraph cache keyed by model, prompt digest and unit id, verified on read.
- **Verification**:
  - [x] The rendered prompt equals the template filled with `indexable_text` and nothing else
  - [x] Each failure kind yields a `failed` record with its reason; nothing is truncated
  - [x] A valid answer round-trips through the cache unchanged; a cache entry for another model or
        prompt digest is not reused
  - [x] No key, header or credential appears in a cache entry

## T6: Sample and batch runners, cost ceiling, `cer extract` [Story: HU-2] [depends on T5]

- **Files**: `src/concept_embeddings_rag/nodes/extraction.py`, `src/concept_embeddings_rag/cli.py`,
  `tests/nodes/test_extraction_runner.py`
- **Action**: an extraction client protocol with a synchronous implementation for the sample and a
  batch implementation for the full run (D4, D5), both lazily importing the SDK and reusing
  `read_api_key`; the estimate from the sample with the margin; the ceiling refusal; persisted batch
  ids; one resubmission of transient failures; the aggregate artifact. `cer extract --sample` and
  `cer extract`.
- **Verification**:
  - [x] With a fake client: the sample takes the 20 lowest unit ids and caches them under the full
        run's keys
  - [x] The full run refuses without a sample, and above the ceiling, before any request
  - [x] Cached paragraphs are never resubmitted; persisted batch ids resume without resubmitting
  - [x] A transient failure is resubmitted exactly once; a second failure is recorded
  - [x] Failures above 1% end the stage with a `[WARN]` naming rate and reasons
  - [x] No network access in any test

## T7: Normalization v1, `NodeIndex`, fragmentation, `cer nodes` [Story: HU-3] [depends on T5] [P]

- **Files**: `src/concept_embeddings_rag/nodes/normalization.py`,
  `src/concept_embeddings_rag/nodes/index.py`, `src/concept_embeddings_rag/cli.py`,
  `tests/nodes/test_normalization.py`, `tests/nodes/test_node_index.py`
- **Action**: `normalize(form)` implementing D6; `build_node_index` from an extraction, with separate
  namespaces and per-paragraph deduplication; save and load with digests; fragmentation statistics
  per type. `cer nodes` refuses an extraction that does not cover every pool unit.
- **Verification**:
  - [x] Each D6 rule has a case (diacritics, casefold, quotes, hyphens, articles, whitespace, empty)
  - [x] Plural forms are **not** folded, asserted
  - [x] The same string as entity and as concept yields two nodes
  - [x] No duplicate node in a row; a modified index is refused on load
  - [x] Fragmentation figures match a hand-counted toy index

## T8: The node hop, with traces [Story: HU-4, HU-6] [depends on T2, T7]

- **Files**: `src/concept_embeddings_rag/evaluation/second_hop.py`,
  `tests/evaluation/test_node_hop.py`
- **Action**: the D9 scoring for an arm's node set, ranking positive candidates by score then unit id
  with read paragraphs excluded, and for each ranked candidate the shared nodes and their
  contributions.
- **Verification**:
  - [x] Scores match a hand-computed toy example for E, C and EC
  - [x] Ties are broken by unit id, not by row position, on a toy pool built to tell them apart
  - [x] Contributions sum to the score for every ranked candidate
  - [x] `p1` and the read paragraphs never appear in a ranking

## T9: The navigation run and `cer navigate` [Story: HU-5, HU-6] [depends on T3, T8]

- **Files**: `src/concept_embeddings_rag/evaluation/navigation.py`,
  `src/concept_embeddings_rag/cli.py`, `tests/evaluation/test_navigation.py`
- **Action**: run every hop over the pilot; per-question and per-paragraph rates; new relevant
  retrievals; concentration; the subgroup; provenance; `HopRun` and `NavigationTrace` saved with
  digests and verified on load.
- **Verification**:
  - [x] Per-question score is the share of that question's missing paragraphs found, on a toy pilot
        with a two-missing question
  - [x] "New relevant" counts exactly the D10 definition on a toy case
  - [x] Every found paragraph within top-100 has a trace, bound to the run digest
  - [x] A test-split question in the pilot is refused

## T10: The gate [Story: HU-7] [depends on T9]

- **Files**: `src/concept_embeddings_rag/evaluation/gate.py`, `tests/evaluation/test_gate.py`
- **Action**: wins, losses, ties and the one-sided binomial p-value per test (D11); the outcome
  function; `GateDecision` saved with the `HopRun` digest; `cer navigate` computes and writes it.
- **Verification**:
  - [ ] Every row of the spec's table has a case: GO; NAMES ONLY; STOP with neither beating the
        comparator; STOP with EC beating it but neither beating E nor E beating it, as an anomaly;
        concept-only beating the comparator alone, as an anomaly
  - [ ] NAMES ONLY is impossible when "E beats BM25" fails
  - [ ] No discordant question gives p = 1.0 and a failed test
  - [ ] The p-value matches `scipy.stats.binomtest` on a known case

## T11: Contamination audit [Story: HU-8] [depends on T3, T6, T9]

- **Files**: `tests/evaluation/test_pilot_contamination.py`
- **Action**: the D13 assertions in one module.
- **Verification**:
  - [ ] Test-split questions refused at the pilot builder and the navigation run
  - [ ] The extraction cache contains no question string of either split (on a fake cache, and on the
        real one when present)
  - [ ] Shuffling gold annotations leaves `p1` and the read top-10 unchanged
  - [ ] An extraction missing any pool unit is refused by `cer nodes`

## T12: REAL — sample, freeze the prompt, checkpoint [Story: HU-2] [depends on T4, T6, T11]

- **Files**: `data/extraction/` (sample records), the plan's step list
- **Action**: `uv run cer extract --sample`; read the schema compliance and measured tokens; freeze
  the prompt; report the measured estimate to the author and wait.
- **Verification**:
  - [ ] All 20 sample paragraphs pass validation, or the prompt is fixed on the sample and the change
        recorded, never after a hop result
  - [ ] The sample report shows its paragraphs' length statistics (tokens: mean, median, min, max)
        beside the corpus's, so the representativeness of the estimate can be judged
  - [ ] The measured estimate is under the ceiling, or the stage stops and the author decides
  - [ ] The prompt digest is committed before the full run
  - [ ] **The author has approved the spend**

## T13: REAL — full extraction [Story: HU-2] [depends on T12]

- **Files**: `data/extraction/extraction-<key>.jsonl.gz` (D7), `.gitignore`
- **Action**: `uv run cer extract`; check the failure rate; version the archive per D7.
- **Verification**:
  - [ ] Every pool unit has a record, `ok` or `failed`
  - [ ] Failure rate is at or below 1%. If it exceeds 1%, the stage stops, the finding is written,
        and no `cer nodes` or navigation run is allowed
  - [ ] Actual cost recorded and under the ceiling
  - [ ] `detect-secrets-hook` passes

## T14: REAL — build the node index [Story: HU-3] [depends on T7, T13]

- **Files**: none versioned beyond the digest recorded later
- **Action**: `uv run cer nodes`; read the fragmentation figures.
- **Verification**:
  - [ ] The index covers all 19,366 units and verifies on load
  - [ ] Fragmentation figures printed per type

## T15: REAL — navigate and apply the gate [Story: HU-5, HU-6, HU-7] [depends on T10, T14]

- **Files**: `data/navigation/` (hop run, traces, gate decision)
- **Action**: `uv run cer navigate`; commit the three artifacts.
- **Verification**:
  - [ ] The four non-concept hops reproduce the diagnostic's paragraph-level figures exactly
  - [ ] The gate outcome is written with its three tests and any anomaly
  - [ ] Nothing is re-run with a changed metric, test, threshold, comparator or set

## T16: Results and the master plan [Story: HU-9] [depends on T15]

- **Files**: `docs/plans/phase_5/5.results.md`, `docs/plans/0_master_plan.md`
- **Action**: write the results from the artifacts, including fragmentation, traces read by the D10
  rule, what was known before the phase, and the synonymy limitation; update the master plan by the
  outcome.
- **Verification**:
  - [ ] Every figure names its artifact and the command that regenerates it
  - [ ] GO opens Phase 6; NAMES ONLY or STOP records the stop and its scope
  - [ ] The founding decisions table untouched unless GO

---

## Status summary

| Task | Story | Status |
|---|---|---|
| T1 | HU-1, HU-2, HU-4, HU-7 | Pending |
| T2 | HU-5 | Pending |
| T3 | HU-1, HU-8 | Pending |
| T4 | HU-1 | Pending |
| T5 | HU-2, HU-8 | Pending |
| T6 | HU-2 | Pending |
| T7 | HU-3 | Pending |
| T8 | HU-4, HU-6 | Pending |
| T9 | HU-5, HU-6 | Pending |
| T10 | HU-7 | Pending |
| T11 | HU-8 | Pending |
| T12 | HU-2 | Pending |
| T13 | HU-2 | Pending |
| T14 | HU-3 | Pending |
| T15 | HU-5, HU-6, HU-7 | Pending |
| T16 | HU-9 | Pending |
