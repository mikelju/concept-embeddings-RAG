# The only secret this project has. Copy to .env and fill it in there.
# Used by the `label` stage alone, for interpretability: no metric depends on it,
# and induction, coding, deduplication and diagnostics all run without it.
ANTHROPIC_API_KEY=
